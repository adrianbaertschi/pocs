# Angular AG Grid Column Selection Demo

This demo shows how to select/highlight a full AG Grid column by clicking any cell in that column.

## Features

- Cell click highlights every cell in that column.
- Row selection is suppressed.
- "Add Column" button appends a new column with generated values.

## Run locally

```bash
npm install
npm start
```

The development server runs on `http://localhost:4300`.
